# RedisME
开源、简洁、智能的跨平台Redis管理客户端


# TODO
- 单机、单机-ssl、集群、集群-ssl都验证连接成功 OK
- 连接配置优化，支持选择文件 OK
- INFO的弹框优化，支持全屏 OK
- 设置客户端名称
- 连接怎么selectDB，是否使用1个连接即可？避免每次都更换数据库
- 打开外部链接，跳转到外部官网
- 1个连接是否需要连接管理器避免断开？是否有必要使用异步连接？
- 标准线程无法取消，是否必要使用异步？
- 终端命令是否需要单独的连接？避免相互影响？
- Command是Sized，是否可以定义个父类？

- tauri第三方组件unwrap导致程序直接退出
- tauri考虑是否需要使用异步模式执行
- 执行命令支持数据库切换
- 弹框支持全屏
- 监控、发布订阅
- Redis的SSL连接
- Redis的哨兵模式
- 自动升级检测支持
- 连接保存到LocalStorage持久化，支持导出导入 ==> 最后处理
- 国际化支持
- 微信登录同步账号信息
